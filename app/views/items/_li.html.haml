%li[item]
	= image_tag(item.icon.url, :alt=>item.name)
	%h3= item.name
	- if can? :update, item
		.actions
			= link_to 'Edit', edit_item_path(item)
			|
			= link_to 'Remove', item, :confirm => 'Are you sure?', :method => :delete